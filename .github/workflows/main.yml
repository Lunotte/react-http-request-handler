/*
 * Filename: c:\Users\Lunotte\Dev\React Native\react-http-request-handler\.github\workflows\main.yml
 * Path: c:\Users\Lunotte\Dev\React Native\react-http-request-handler
 * Created Date: Sunday, August 8th 2021, 6:07:18 pm
 * Author: Charly Beaugrand
 * 
 * Copyright (c) 2021 Lunotte
 */

#name: Build
#on:
#  push:
#    branches:
#      - master
#  pull_request:
#    branches:
#      - master
#    types: [opened, synchronize, reopened]
#jobs:
#  sonarcloud:
#    name: SonarCloud
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v2
#        with:
#          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
#      - name: SonarCloud Scan
#        uses: SonarSource/sonarcloud-github-action@master
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed to get PR information, if any
#          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
